# BTC Capital Pulse · 体验约定

首页负责扫描，详情负责验证。主线为：结论 → 图表依据 → 接下来观察的条件。

- BTC 单资产入口，保留 SOXX 外链，不生成缺乏样本依据的综合排名。
- 默认新手模式：普通语言、少量结论。专业模式展开计算读数，不改变判定结果。
- 首页为价格位置图与趋势、资金、期权、波动四项紧凑清单。详细视图按标签切换。
- 微软雅黑；手机无整页横向滚动。标签可单行横向滚动，图表纵向手势交还页面。
- 重要图表可放大，移动原图以保留交互；关闭恢复位置与焦点。
- 虚线术语打开底部说明弹层。说明包含来源、方法、范围、日期、用途边界及平台差异。
- 保留一处页脚免责声明，数据日期清楚呈现。缺失值不替换为演示数据。
- 系统支持时直接分享图片与链接；否则保存图片、复制链接，不经过海报编辑流程。

## 数据约定

- 看涨墙 / 看跌墙：Deribit 所选到期日的 Call / Put OI 最大行权价，独立于 Gamma 可计算样本。全 OI 覆盖不完整时只展示已知样本，不输出完整墙结论。
- Gamma 是模型代理，不是实际做市商持仓；没有严格 Gamma Flip 推断。各到期日状态独立，过期或无效样本清空图表。
- 平值 IV：最接近远期价的上市行权价，双侧标记 IV 平均；缺一侧则不输出比较或区间。
- 历史波动：近似同期限 N 天连续日对数收益样本标准差，按 365.25 天年化。N 为剩余天数四舍五入，支持 7–90 天；日 K 过期或不连续则暂停比较。
- 简化区间：指数代理价 ± 指数代理价 × IV × sqrt(剩余天数 / 365.25)，不保证覆盖率，不直接称贵或便宜。
- 趋势：已收盘价格、20 日简单均线、60 日简单均线的排列，不以实时跳动替代日线收盘。
- 新鲜度与传输方式分开判断：最近成功的缓存仍保留原始时间，不因重新取到缓存而延长有效期。
- 静态快照原位刷新，不整页跳转，不重置阅读位置和到期日。

## 验证

```text
node scripts/test-experience.mjs
node scripts/test-deribit-options.mjs
node scripts/validate-static-data.mjs
node scripts/build-pages.mjs
node scripts/validate-pages.mjs
```

界面回归覆盖：360/390/768/1440px、直达链接、返回导航、标签与到期日切换、原图重复放大、弹层关闭与焦点、图片分享回退。测试期权样本只注入独立测试浏览器，不写入生产数据或构建产物。
